<h2>Edit Listing</h2>

<%= render "form.html", changeset: @changeset,
                        action: admin_event_path(@conn, :update, @show) %>

<span><%= link "Back", to: admin_event_path(@conn, :index) %></span>
